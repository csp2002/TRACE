# Core ML
torch>=1.13
torchvision>=0.14
numpy>=1.23
scipy>=1.10
scikit-learn>=1.2
scikit-image>=0.20

# Imaging
Pillow>=9.4
opencv-python>=4.7
nibabel>=5.0
SimpleITK>=2.2

# Plotting & utilities
matplotlib>=3.7
tqdm>=4.65
einops>=0.6
pyyaml>=6.0
ml-collections>=0.1.1

# Foundation-model deps (third_party/medsam, third_party/medsam2)
# Install separately when running MedSAM / MedSAM2 backbones; see medsam.yaml.

# Pretrained backbone weights (TransUNet, SwinUNet ImageNet inits) need to be
# downloaded separately — see docs/data_preprocessing.md.
